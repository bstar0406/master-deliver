import * as React from "react";

import Template from "./Template";

const GoodItemController = (props) => {
  return (
    <Template
      item={props.item}
    />
  );
};
export default GoodItemController;
