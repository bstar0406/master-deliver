import * as React from "react";

import Template from "./Template";

const RecipientListController = (props) => {
  return (
    <Template setPage={props.setPage}/>
  );
};
export default RecipientListController;
