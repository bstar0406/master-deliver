import * as React from "react";

import Template from "./Template";
const BoxTableItemController = (props) => {
  return <Template item={props.item} link={props.link} />;
};
export default BoxTableItemController;
