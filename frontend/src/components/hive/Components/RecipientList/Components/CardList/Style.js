import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  searchText:{
    height:"40px",
    marginTop:"-15px"
  },
  colorDust:{
    color:"#384182"
  }
});

export { useStyles };
