import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  plusButton: {
    width: "45px",
    height: "45px",
    position: "absolute",
    top: "153px",
    right: "15px",
  },
});

export { useStyles };
